---
description: Android ProdRelease 用户包打包硬约束、白屏/闪退排查（Nitro、polyfill、图片资源名）
globs: patches/**/*,packages/shared/src/polyfills/**/*,apps/mobile/android/**/*,apps/mobile/scripts/build-user-release-apk.sh,apps/mobile/scripts/inject-release-js-into-apk.sh,node_modules/react-native-nitro-modules/**/*,node_modules/react-native-url-polyfill/**/*
alwaysApply: false
---

# Android Release 启动白屏 / 闪退

模拟器 Debug+Metro 能开 ≠ ProdRelease 真机能开。白屏优先用真机 Release + logcat，不要靠全量半小时打包盲猜。

## 已知根因链（2026-08）

1. `url-polyfill`：`NativeModules.BlobModule` 顶层解构 → Release bridgeless 下 `NativeModules` 可能为 `undefined`
2. Nitro：`NitroModules.createHybridObject` 在 install 完成前被 hybrid 顶层调用 → `createHybridObject of undefined`
3. 假 stub 危险：JS Proxy stub 骗过 (2) 后，`installWorkletsSupport` 对 stub/`Proxy` 做 `box()` → 原生 **SIGSEGV**

正确方向：`ensureInstalled()` 后转发到 `global.NitroModulesProxy`；`box()` 只 box 原生 proxy，不要造假 HybridObject。

## 定位（秒级）

```bash
adb logcat -c
adb shell am start -n so.onekey.app.wallet/.MainActivity
adb logcat -d | rg -i 'BlobModule|createHybridObject|TypeError|SIGSEGV|ReactNativeJS|Abort message'
```

好用节点对比：用 `git log` / `git diff` 看 `polyfills/`、`patches/`、启动入口，不必对业务 UI 做 13 提交盲 bisect。

## 快速验证（勿每次 assembleProdRelease）

1. 改 JS / patch / `node_modules` 后，**删掉**再打主包，避免 `createBundle* UP-TO-DATE`：
   ```bash
   rm -f apps/mobile/android/app/build/generated/assets/createBundleProdReleaseJsAndAssets/index.android.bundle
   cd apps/mobile/android && ./gradlew :app:createBundleProdReleaseJsAndAssets -x lint -x test
   ```
2. 确认字符串进了 **index**（不是只有 background）：
   ```bash
   strings …/index.android.bundle | rg 're-entrant access|ensureInstalled|resolveBlobModule'
   ```
3. 注入现有完整 ProdRelease APK（debug.keystore）并安装：
   ```bash
   DEVICE=<serial> apps/mobile/scripts/inject-release-js-into-apk.sh /path/to/good-base.apk
   ```
4. **禁止**全量解压再重打 zip（会弄坏 binary Manifest，如 expo clipboard meta-data）；脚本须原地 `zip -d/-u` 替换 `assets/*.bundle`。

## 给用户发版（避免黑屏 / 闪退 + 按文件名热更）

用户包必须是 **ProdRelease**，JS 打进 APK，文件名必须是 `app-{VERSION}.apk`。

**禁止：** `yarn app:android`、ProdDebug、等 Metro 的包（整页黑）；`ENABLE_NATIVE_BACKGROUND_THREAD=false` 却打了双线程 JS（底栏有、中间黑）；注入时丢掉 `common.bundle` / 把 `background.bundle` 打成十几 KB（闪退或中间黑）。

**每次打包（改了 JS / 图片后必须整包重跑，禁止只注入某一个 `.bundle`）：**

```bash
# 第一包（VERSION=1.0.0 → dist/android/app-1.0.0.apk）
bash apps/mobile/scripts/build-user-release-apk.sh

# 之后每一包自动 +0.0.1 并改名（1.0.1、1.0.2…）
bash apps/mobile/scripts/build-user-release-apk.sh --bump-patch
```

脚本会：`UNION_BUILD` 打完整 JS → `SKIP_EXPO_JS_BUNDLE` 组装 → 校验 APK 内 `index`/`common`/`background`（background ≥ 1MB）和钱包卡 / Tab 资源名 → 复制为 `app-{VERSION}.apk`。`versionName`/`versionCode` 跟 `.env.version` 的 `VERSION`。

**打包硬约束（1.0.3 已验证；违反会再空白 / 黑屏）：**

- 不要关 `ENABLE_NATIVE_BACKGROUND_THREAD`（脚本已 export true；`.env` 默认常为 false）
- 不要去掉 `SKIP_EXPO_JS_BUNDLE` 在 `android/app/build.gradle` `afterEvaluate` 里把 Union `drawable-*`/`raw/` 拷进 `generated/res/createBundleProdReleaseJsAndAssets` 的逻辑
- 不要删 `getAndroidPackedResourceName`（`packages/shared/src/polyfills/assetResolutionPatch.js`）：Union 的 `/assets/../../packages/...` 必须 collapse 成 `packages_*`，否则钱包背景 / Tab 图标空白（图在 APK 里也对不上 JS 资源名）

上传到网站根目录（与客户端检测一致）：

`https://www.moblus.net/app-1.0.1.apk`

首页是 SPA，没有目录列表；客户端会按文件名 HEAD `app-x.y.z.apk`。Nginx 必须对 `.apk` 返回真实文件，不能 fallback 成 `index.html`。签名必须与已安装包相同（当前 Release 用 debug.keystore）。

装完 **force-stop 冷启动**，必须看到引导页/资产，不能只有 Tab。

---

成品必须是 **ProdRelease**，JS 打进 APK，不依赖电脑 Metro。

| 用户看到 | 原因 | 禁止 |
|---|---|---|
| 整页黑、等开发服务器 | 装了 `ProdDebug` / `yarn app:android` | 把 Debug 当用户包 |
| 白屏 / 秒崩 SIGSEGV | Nitro `createHybridObject`、url-polyfill `BlobModule`、注入时删了 `common.bundle` 却没加回 | 残缺注入；假 Nitro stub |
| 底栏 5 Tab 正常、中间黑 | 原生 `ENABLE_NATIVE_BACKGROUND_THREAD=true` 但只有一条 JS 线程；或 Home 在 `ready===false` 时只渲染 Header；或 `freezeOnBlur` + 原生底栏 | 双线程 JS 对单线程 so；只看「进程活着/有 Tab」就宣称成功 |
| Gradle 卡死 / 分段成环 | Expo `createBundleProdReleaseJsAndAssets` + 循环 segment DAG | 每次全量盲打半小时；`prepare-android-build.sh` 里 `gradlew --stop` |
| 钱包背景 / Tab 图标空白 | 没拷 Union `drawable-*`/`raw/`；或 JS 查 `__packages_*` 而 APK 是 `packages_*` | 去掉 SKIP_EXPO 拷贝；删 `getAndroidPackedResourceName`；只注入某一个 `.bundle` |

正确打包：

1. Union JS：`UNION_BUILD=true SPLIT_BUNDLE=1`，`common.bundle` 数 MB、`background.bundle` **不能是十几 KB**、带 `segments/` 与 `segments-background/`。
2. 原生开关必须与 JS 一致：双线程 JS ↔ `ENABLE_NATIVE_BACKGROUND_THREAD=true`（`.env` 默认常为 false，assemble 时要显式 export）。
3. Expo Metro 分段成环时：先 `node build-bundle.js --platform android`，再 `SKIP_EXPO_JS_BUNDLE=true ./gradlew :app:assembleProdRelease`（见 `app/build.gradle`）。跳过 Expo Metro 时必须把 union 的 `dist/assets/drawable-*` 和 `raw/` 写进 `generated/res/createBundle*JsAndAssets`（`createBundle` 的 doFirst），否则 APK 有 JS 但钱包背景 / Tab 图标空白。Android 必须 `patchAndroidApkResourceIdentifierResolution`，且资源名要 **collapse `/assets/../../packages` → `packages_*`**：RN `getAndroidResourceIdentifier` 会算出 `__packages_kit_assets_home_walletcard`，saveAssets/aapt 打进去的是 `packages_kit_assets_home_walletcard`，对不上就会空白。只改 file:// / 只调用未 collapse 的 `resourceIdentifierWithoutScale()` 不够。
4. 注入必须同步 `index` + `common` + `background` + **segments**（`inject-release-js-into-apk.sh`）；`zip -d` 掉 `common.bundle` 必须立刻加回。
5. 装上后 **force-stop 冷启动**，截图必须看到引导页/资产内容，不能只有 Tab。签名用 `debug.keystore` 时，与旧包冲突先卸载。

## 补丁纪律

- 改 `node_modules` 后必须重生成 `patches/<pkg>+<ver>.patch` 并 `git apply --check`
- patch 不得含 `android/build`、`android/bin`、`.gradle`
- Metro 对 nitro 走 `"react-native": "src/"`，但 patch 应同步 `src` + `lib/module` + `lib/commonjs`

## 案例

详见 `.claude/skills/1k-retrospective/references/case-studies.md`（BlobModule / createHybridObject / box SIGSEGV）。
